import 'package:flutter/material.dart';

class PerformanceChartGrid extends StatelessWidget {
  const PerformanceChartGrid({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
